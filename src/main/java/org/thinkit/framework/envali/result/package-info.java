/**
 * The package that manages the classes that represent the results of the
 * validation returned by the Envali framework.
 * <p>
 * An {@link BusinessError} object will be created when specifying an error type
 * (recoverable or unrecoverable) when annotating the Envali framework. An empty
 * {@link ValidationResult} object is created if there are no errors, regardless
 * of the error type.
 */
package org.thinkit.framework.envali.result;
